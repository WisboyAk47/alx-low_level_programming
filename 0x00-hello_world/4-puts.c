#include <stdio.h>
/**
 * main - prints a line with puts function
 *
 * Description: This program uses the puts function to print a line of text
 *              to the standard output. The line includes escaped quotes
 *              and a newline character.
 *
 * Return: Always 0 (success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
